import React from "react";
import { Routes, Route } from "react-router-dom";
import App from "../App";

const HomePage = () => {
  return (
    <Routes>
      <App />
    </Routes>
  );
};

export default HomePage;
